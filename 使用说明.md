# CloudStudio 使用说明

## 项目启动

1. **安装依赖**:
```bash
cd cloud-studio
npm install
```

2. **启动开发服务器**:
```bash
npm run dev
```

3. **访问项目**:
打开浏览器访问 `http://localhost:5174`

## 功能测试

### 1. 用户认证测试

**登录测试**:
- 访问登录页面
- 使用测试账号：
  - 邮箱: `admin@cloudstudio.com`
  - 密码: `123456`
- 点击登录按钮

**注册测试**:
- 点击"立即注册"链接
- 填写注册信息
- 提交注册（会显示成功消息）

**密码重置测试**:
- 点击"忘记密码"链接
- 输入邮箱地址
- 点击发送重置链接

### 2. 工作空间管理测试

**查看工作空间列表**:
- 登录成功后自动跳转到工作台
- 可以看到4个示例工作空间
- 不同状态的工作空间显示不同的操作按钮

**创建工作空间**:
- 点击右上角"新建"按钮
- 填写工作空间信息
- 选择开发环境和规格配置
- 点击"新建"按钮

**管理工作空间**:
- **启动**: 点击"启动"按钮启动已停止的工作空间
- **停止**: 点击"停止"按钮停止运行中的工作空间
- **打开**: 点击"打开"按钮跳转到code-server编辑器
- **删除**: 点击更多菜单中的"删除"选项

**查看工作空间详情**:
- 点击更多菜单中的"设置"选项
- 查看工作空间的详细信息、资源使用情况和操作日志

### 3. 用户中心测试

**个人资料管理**:
- 点击右上角用户头像
- 选择"个人资料"
- 修改用户信息
- 修改密码

## 项目特性

### ✅ 已实现功能

1. **完整的用户认证系统**
   - 登录、注册、密码找回
   - JWT Token认证
   - 路由守卫保护

2. **现代化UI设计**
   - 深色企业级主题
   - 响应式布局
   - Element Plus组件库
   - 流畅的交互动画

3. **工作空间管理**
   - 创建、删除、启动、停止
   - 状态监控和资源统计
   - 操作日志记录
   - code-server集成

4. **API接口完整**
   - RESTful API设计
   - 模拟数据支持
   - 错误处理机制
   - 请求拦截器

### 🔄 技术架构

- **前端框架**: Vue 3 + Composition API
- **状态管理**: Pinia
- **路由管理**: Vue Router 4
- **UI组件**: Element Plus
- **HTTP客户端**: Axios
- **构建工具**: Vite
- **样式方案**: SCSS

### 📱 响应式支持

- 桌面端优化显示
- 平板端自适应布局
- 移动端友好界面
- 触摸操作支持

## 后端对接

项目已预留完整的API接口，后端开发者可以参考 `API接口文档.md` 实现对应的服务端接口。

### 主要接口：

1. **用户认证**:
   - `POST /api/auth/login` - 登录
   - `POST /api/auth/register` - 注册
   - `POST /api/auth/reset-password` - 重置密码

2. **工作空间管理**:
   - `GET /api/workspaces` - 获取列表
   - `POST /api/workspaces` - 创建工作空间
   - `POST /api/workspaces/{id}/start` - 启动
   - `POST /api/workspaces/{id}/stop` - 停止
   - `DELETE /api/workspaces/{id}` - 删除

### 模拟API

当前项目使用模拟API，位于 `src/mock/index.js`，包含：
- 完整的请求拦截
- 真实的数据结构
- 状态管理逻辑
- 错误处理机制

## 部署说明

### 开发环境
```bash
npm run dev
```

### 生产构建
```bash
npm run build
```

### 预览构建结果
```bash
npm run preview
```

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 问题反馈

如果在使用过程中遇到问题，请检查：

1. Node.js版本是否 >= 16
2. 依赖是否正确安装
3. 端口是否被占用
4. 浏览器控制台是否有错误信息

## 开发建议

1. **代码规范**: 使用ESLint和Prettier保持代码风格一致
2. **组件复用**: 提取公共组件，提高代码复用率
3. **性能优化**: 使用懒加载和代码分割优化加载速度
4. **测试覆盖**: 添加单元测试和集成测试
5. **文档维护**: 及时更新API文档和使用说明